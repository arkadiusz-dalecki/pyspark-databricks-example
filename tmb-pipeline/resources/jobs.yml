resources:
  jobs:
    tmb_analysis_job:
      name: tmb-analysis-${bundle.target}

      tasks:
        - task_key: analyze
          environment_key: default
          spark_python_task:
            python_file: ../src/tmb_analysis.py

      environments:
        - environment_key: default
          spec:
            client: "2"
